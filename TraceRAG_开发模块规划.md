# TraceRAG 开发模块规划

## 项目目标

TraceRAG 是一个面向 AI 应用开发 / 大模型应用工程师求职展示的可溯源企业知识库问答系统。

项目计划在 7–14 天内完成 MVP，重点体现以下工程能力：

- 文档导入与结构化处理
- 检索增强生成（RAG）完整链路
- 回答引用与来源追踪
- 证据不足时可靠拒答
- 离线评测与 Bad Case 驱动迭代
- API、简单界面与容器化交付

项目不追求复杂产品功能，暂不包含用户系统、权限、支付、组织空间等非核心模块。

## 开发模块

### 模块 1：项目骨架与配置

**目标：** 建立项目基础结构，使服务能够正常启动。

**重点：** 确定目录结构、依赖管理、配置管理、日志方式、环境变量和基础 FastAPI 应用。

**验收标准：** FastAPI 服务能够启动，`/health` 接口正常返回。

### 模块 2：文档加载与统一数据模型

**目标：** 支持 PDF、Markdown、TXT 文档，并统一转换为内部数据结构。

**重点：** 从文档加载阶段开始保留文件名、PDF 页码等来源信息。

**验收标准：** 输入一个 PDF 后，能够输出每页文本、文件名和页码。

### 模块 3：文本清洗与 Chunk 切分

**目标：** 完成基础文本清洗与带重叠的 Chunk 切分，并生成唯一的 `chunk_id`。

**重点：** 每个 Chunk 必须保留 `file_name`、`page`、`document_id`、`chunk_index` 等元数据。

**验收标准：** 文档能够转换为结构化 Chunks，且每个 Chunk 的来源均可追踪。

### 模块 4：Embedding 与向量索引

**目标：** 接入 OpenAI 兼容的 Embedding API，并使用 FAISS 建立向量索引。

**重点：** 保证向量与 Chunk、元数据之间的映射稳定且可持久化。

**验收标准：** 文档可以成功向量化；索引能够保存并重新加载。

### 模块 5：基础 Vector Retriever

**目标：** 实现从查询到 Top-K Chunks 的基础向量检索链路，暂不接入 LLM。

**重点：** 检索结果需返回文本、元数据和相关性分数。

**验收标准：** 对准备好的测试问题进行检索，人工检查 Top-K 结果能够命中正确证据。

### 模块 6：RAG 生成、引用与拒答

**目标：** 接入 LLM，使其仅依据检索证据回答，并提供引用和第一版低置信度拒答能力。

**重点：** 回答必须能够映射到具体来源；证据不足时不得编造答案。

**验收标准：** 知识库内的问题能够得到带引用的回答；知识库外或证据不足的问题能够明确拒答。

### 模块 7：FastAPI 与 Streamlit Demo

**目标：** 将稳定的 RAG Pipeline 封装为 API，并提供简单的交互界面。

**重点：** 串联文档上传、提问、回答、引用和检索证据展示。

**验收标准：** 用户能够在浏览器中完成“上传文档 → 提问 → 查看回答、引用与检索证据”的完整流程。

### 模块 8：离线评测体系

**目标：** 建立约 50 条测试数据，评估检索、回答、引用和拒答效果。

**重点：** 至少覆盖 Recall@K、检索命中率、回答正确性、引用一致性和拒答准确性，并保留评测结果。

**验收标准：** 通过一个命令即可运行评测并生成 Baseline 结果。

### 模块 9：BM25、混合检索与 Bad Case 迭代

**目标：** 根据 Baseline 的失败案例加入 BM25，并使用 RRF 等方式实现混合检索和策略迭代。

**重点：** 以真实 Bad Case 和评测数据说明为什么改进，以及改进带来了什么效果。

**验收标准：** 获得 Vector Baseline 与 Hybrid Retrieval 的量化对比，以及有代表性的 Bad Case 前后对比。

### 模块 10：工程收尾与求职展示

**目标：** 完善项目交付质量，使陌生人能够理解、启动、测试并评估项目。

**重点：** 完成 Docker 一键启动、pytest、README、架构图、Demo、评测结果和简历表述；仅在前九个模块稳定后考虑加入 Rerank。

**验收标准：** 项目能够被顺利克隆、启动和测试，文档能够清楚说明系统设计、评测方法和迭代成果。

## 开发原则

采用逐模块开发和验收的方式：**前一模块验收通过后，再进入下一模块。**

每个阶段优先保证当前链路真实可用、结果可检查、问题可定位，不用后续模块掩盖前序问题。特别是基础检索尚未达到验收标准时，不应依赖 LLM 生成来掩盖检索缺陷。
